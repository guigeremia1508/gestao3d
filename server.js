require('dotenv').config();const express=require('express'),cors=require('cors'),path=require('path'),{initDb}=require('./database/init');
const app=express(),PORT=process.env.PORT||3000;app.use(cors());app.use(express.json({limit:'2mb'}));app.use(express.urlencoded({extended:true,limit:'2mb'}));app.use(express.static(path.join(__dirname,'frontend')));app.use('/api/auth',require('./routes/auth'));app.use('/api',require('./routes/api'));app.use('/api',(err,req,res,next)=>{console.error('API error:',err);res.status(err.status||500).json({error:err.message||'Erro interno do servidor.'})});app.use('/api',(req,res)=>res.status(404).json({error:'Rota da API não encontrada.'}));app.get('*',(req,res)=>res.sendFile(path.join(__dirname,'frontend','index.html')));
initDb().then(()=>app.listen(PORT,'0.0.0.0',()=>console.log(`🖨️ Gestão 3D v2 rodando na porta ${PORT}`))).catch(e=>{console.error('Erro ao inicializar PostgreSQL:',e);process.exit(1)});
